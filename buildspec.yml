version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - echo Installing dependencies...
      - npm install -g yarn
      - yarn install

  pre_build:
    commands:
      - echo Running tests...
      - yarn test:ci
      - echo Setting environment variable REACT_APP_SERVER from CodeBuild parameters...
      - export REACT_APP_SERVER=$REACT_APP_SERVER
      - echo REACT_APP_SERVER is set to $REACT_APP_SERVER

  build:
    commands:
      - echo Build started on `date`
      - yarn build
      - echo Build completed on `date`

artifacts:
  files:
    - '**/*'
  base-directory: build
  name: ws-$(date +%Y%m%d-%H%M%S)

cache:
  paths:
    - 'node_modules/**/*'
